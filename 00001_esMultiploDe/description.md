Definí la función `esMultiploDe`, que dados dos números devuelve si el primero es múltiplo del segundo (que sorpresa, ¡eh!), **utilizando aplicación parcial y composición**. Ejemplo:

```haskell
ム esMultiploDe 12 3
True
```

